# Database
QUARKUS_DATASOURCE_JDBC_URL=jdbc:postgresql://127.0.0.1:5432/trackademy_bd
QUARKUS_DATASOURCE_USERNAME=trackademy
QUARKUS_DATASOURCE_PASSWORD=change_me

# HTTP bind
QUARKUS_HTTP_HOST=127.0.0.1
QUARKUS_HTTP_PORT=8080

# Microsoft OIDC / JWT validation
# Keep false only if you intentionally run without token validation.
QUARKUS_OIDC_TENANT_ENABLED=true
QUARKUS_OIDC_APPLICATION_TYPE=service
QUARKUS_OIDC_AUTH_SERVER_URL=https://login.microsoftonline.com/<TENANT_ID>/v2.0
QUARKUS_OIDC_CLIENT_ID=<TRACKADEMY_API_CLIENT_ID>
QUARKUS_OIDC_TOKEN_AUDIENCE=api://<TRACKADEMY_API_CLIENT_ID>

# CORS for local frontend
QUARKUS_HTTP_CORS=true
QUARKUS_HTTP_CORS_ORIGINS=http://localhost:4200
QUARKUS_HTTP_CORS_HEADERS=accept,authorization,content-type,x-requested-with
QUARKUS_HTTP_CORS_METHODS=GET,POST,PUT,DELETE,OPTIONS
